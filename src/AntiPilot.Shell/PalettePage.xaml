<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AntiPilot.Shell.PalettePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AntiPilot.Shell">

    <Grid RowDefinitions="Auto,*">
        <TextBlock Text="{local:Str Key=PaletteIntro}" Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="2,0,2,10" />

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="6">
            <Grid RowDefinitions="Auto,*,Auto">
                <Grid ColumnDefinitions="40,220,*" Margin="12,6,12,4">
                    <TextBlock Text="#" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Grid.Column="1" Text="{local:Str Key=PaletteColumnLabel}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Grid.Column="2" Text="{local:Str Key=PaletteColumnAction}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Grid>
                <ListView x:Name="Entries" Grid.Row="1" SelectionMode="Single" DoubleTapped="OnEdit"
                          AutomationProperties.Name="{local:Str Key=TabPalette}" />
                <TextBlock x:Name="Empty" Grid.Row="1" Text="{local:Str Key=PaletteNoEntries}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Collapsed" />
                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="6,8,6,2">
                    <Button Content="{local:Str Key=PaletteAdd}" Click="OnAdd" />
                    <Button Content="{local:Str Key=Edit}" Click="OnEdit" />
                    <Button Content="{local:Str Key=Remove}" Click="OnRemove" />
                    <Button Content="{local:Str Key=PaletteMoveUp}" Click="OnMoveUp" />
                    <Button Content="{local:Str Key=PaletteMoveDown}" Click="OnMoveDown" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
